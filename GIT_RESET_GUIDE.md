# 🔄 HƯỚNG DẪN: Gỡ Git và tạo lại từ đầu

## ⚠️ LƯU Ý QUAN TRỌNG:
- **SAO LƯU** project trước khi thực hiện
- Nếu đã push lên GitHub/GitLab, bạn sẽ mất lịch sử commit cũ
- Nếu muốn giữ lịch sử, hãy tạo branch mới thay vì xóa Git

---

## 📋 CÁC BƯỚC THỰC HIỆN:

### **Bước 1: Xóa Git repository hiện tại**

Mở **Terminal/PowerShell** trong thư mục project và chạy:

```powershell
# Di chuyển vào thư mục project
cd D:\BTLAndroid\Tech_App

# Xóa thư mục .git (ẩn)
Remove-Item -Recurse -Force .git

# Kiểm tra lại
if (Test-Path .git) {
    Write-Host "Git vẫn còn tồn tại"
} else {
    Write-Host "Đã xóa Git thành công"
}
```

**Hoặc dùng Command Prompt:**
```cmd
cd D:\BTLAndroid\Tech_App
rmdir /s /q .git
```

---

### **Bước 2: Tạo file .gitignore (nếu chưa có)**

Tạo file `.gitignore` ở thư mục gốc project:

```gitignore
# Built application files
*.apk
*.aar
*.ap_
*.aab

# Files for the ART/Dalvik VM
*.dex

# Java class files
*.class

# Generated files
bin/
gen/
out/
release/

# Gradle files
.gradle/
build/
*/build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/

# Android Studio captures folder
captures/

# IntelliJ
*.iml
.idea/
misc.xml
deploymentTargetDropDown.xml
render.experimental.xml

# Keystore files
*.jks
*.keystore

# External native build folder generated in Android Studio 2.2 and later
.externalNativeBuild
.cxx/

# Google Services (e.g. APIs or Firebase)
google-services.json

# Freeline
freeline.py
freeline/
freeline_project_description.json

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output
fastlane/readme.md

# Version control
vcs.xml

# lint
lint/intermediates/
lint/generated/
lint/outputs/
lint/tmp/

# Android Profiling
*.hprof

# OS-specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Markdown files (tùy chọn - nếu bạn muốn commit các file .md)
# *.md
```

---

### **Bước 3: Tạo Git repository mới**

```powershell
# Khởi tạo Git repository mới
git init

# Thêm tất cả files
git add .

# Commit lần đầu
git commit -m "Initial commit - TechStore Android App"
```

---

### **Bước 4: Kết nối với remote repository (GitHub/GitLab)**

#### **Nếu tạo repository mới trên GitHub:**

1. Vào GitHub → New Repository
2. Tên: `TechStore-Android` (hoặc tên bạn muốn)
3. **KHÔNG** tích "Initialize with README"
4. Copy URL (ví dụ: `https://github.com/username/TechStore-Android.git`)

#### **Kết nối và push:**

```powershell
# Thêm remote
git remote add origin https://github.com/username/TechStore-Android.git

# Đổi tên branch thành main (nếu cần)
git branch -M main

# Push lên GitHub
git push -u origin main
```

---

### **Bước 5: Xác nhận**

```powershell
# Kiểm tra remote
git remote -v

# Kiểm tra status
git status

# Xem lịch sử commit
git log --oneline
```

---

## 🔧 NẾU GẶP LỖI:

### **Lỗi: "remote origin already exists"**
```powershell
# Xóa remote cũ
git remote remove origin

# Thêm lại
git remote add origin <URL_MỚI>
```

### **Lỗi: "failed to push some refs"**
```powershell
# Force push (CẨN THẬN - chỉ dùng khi chắc chắn)
git push -u origin main --force
```

### **Lỗi: "Authentication failed"**
- Kiểm tra username/password
- Hoặc dùng Personal Access Token thay vì password

---

## ✅ CHECKLIST:

- [ ] Đã sao lưu project
- [ ] Đã xóa thư mục `.git`
- [ ] Đã tạo file `.gitignore`
- [ ] Đã chạy `git init`
- [ ] Đã chạy `git add .`
- [ ] Đã commit lần đầu
- [ ] Đã tạo repository trên GitHub/GitLab
- [ ] Đã thêm remote
- [ ] Đã push thành công

---

## 📝 LỆNH NHANH (Copy toàn bộ):

```powershell
# 1. Xóa Git cũ
cd D:\BTLAndroid\Tech_App
Remove-Item -Recurse -Force .git -ErrorAction SilentlyContinue

# 2. Tạo Git mới
git init
git add .
git commit -m "Initial commit - TechStore Android App"

# 3. Kết nối remote (THAY <URL> bằng URL thật của bạn)
git remote add origin <URL>
git branch -M main
git push -u origin main
```

---

## 🎯 KẾT QUẢ:

Sau khi hoàn thành, bạn sẽ có:
- ✅ Git repository mới, sạch sẽ
- ✅ Không còn lịch sử commit cũ
- ✅ Code đã được push lên GitHub/GitLab
- ✅ Sẵn sàng để làm việc từ đầu



<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.app_store">

    <!-- ⚙️ Quyền truy cập -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_store"
        tools:targetApi="31">

        <!-- 🌟 LoginActivity là màn hình khởi động đầu tiên -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🌟 Màn hình chính của người dùng -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:launchMode="singleTop" />

        <!-- 🔹 4 màn chính có bottom navigation -->
        <activity
            android:name=".activities.CartActivity"
            android:launchMode="singleTop"
            android:exported="false" />

        <activity
            android:name=".activities.OrderHistoryActivity"
            android:launchMode="singleTop"
            android:exported="false" />

        <activity
            android:name=".activities.OrderDetailsActivity"
            android:launchMode="singleTop"
            android:exported="false" />

        <activity
            android:name=".activities.AccountActivity"
            android:launchMode="singleTop"
            android:exported="false" />

        <!-- 🔹 Các màn phụ -->
        <activity android:name=".activities.RegisterActivity" android:exported="false" />
        <activity android:name=".activities.ForgotPasswordActivity" android:exported="false" />
        <activity android:name=".activities.ProductDetailActivity" android:exported="false" />
        <activity android:name=".admin.AdminRevenueActivity" />
        <activity android:name=".activities.ChangePasswordActivity" />
        <activity android:name=".activities.ViewImageActivity"
            android:exported="false" />
        <activity
            android:name=".admin.AdminReviewActivity"
            android:exported="false" />
        <activity
            android:name=".admin.AdminPromotionActivity"
            android:exported="false" />

        <!-- 🔹 Màn hình dành cho Admin -->
        <activity android:name=".admin.AdminDashboardActivity" android:exported="false" />
        <activity android:name=".admin.EditProductActivity" android:exported="false" />
        <activity android:name=".admin.AdminProductActivity" android:exported="false" />
        <activity android:name=".admin.AdminUserActivity" android:exported="false" />
        <activity android:name=".admin.AdminOrderActivity" android:exported="false" />
        <activity android:name=".admin.AdminOrderDetailsActivity" android:exported="false" />

    </application>

</manifest>
